{
  "_comment": "MCP (Model Context Protocol) 服务发现配置. 主后端启动时会读这个文件, 自动启动列出的 MCP 服务.",
  "mcpServers": {
    "autohire-resume-parser": {
      "_comment": "AutoHire 的简历/JD 解析服务, 独立进程, 通过 stdio 与主后端通信",
      "command": "python",
      "args": ["-m", "mcp_servers.resume_server"],
      "cwd": "../backend",
      "env": {
        "PYTHONPATH": "../backend"
      },
      "description": "简历 + JD 解析 MCP 服务 (FastMCP, stdio, 独立进程)"
    }
  }
}